{
  "name": "vintage-watch-manual",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-data": "node scripts/generate-manifest.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/tailwind": "^6.0.0",
    "astro": "^5.0.0",
    "astro-pagefind": "^1.8.0",
    "pagefind": "^1.3.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "glob": "^11.0.0",
    "wrangler": "^4.83.0"
  }
}
