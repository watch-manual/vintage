# Start Dev Server

Start the Astro development server for local preview.

## Command

```bash
npm run dev
```

## Details

- Dev server runs at http://localhost:4321
- If `src/data/calibers.json` is missing, run `npm run generate-data` first.
